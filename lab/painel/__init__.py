"""The local panel: configure the agents, run them, and read what they did.

Kept out of ``lab`` proper so that importing the library never drags Streamlit in. The
panel is optional; the CLI and the metrics are not.
"""
